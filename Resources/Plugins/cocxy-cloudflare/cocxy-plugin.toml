name = "Cloudflare Helper"
description = "Adds local Cloudflare CLI checks and command helpers."
version = "1.0.0"
author = "Cocxy"
repository = "bundled://cocxy-cloudflare"
license = "MIT"
events = ["session-start"]
capabilities = ["environment-read", "process-spawn"]
