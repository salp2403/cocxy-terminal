name: Performance

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  cold-start:
    name: Cold start smoke budget
    runs-on: macos-15
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Install xcodegen
        run: brew install xcodegen

      - name: Build app bundle
        run: ./scripts/build-app.sh debug

      - name: Measure cold start
        run: |
          chmod +x scripts/bench-cold-start.sh
          scripts/bench-cold-start.sh --app build/CocxyTerminal.app --runs 5 --enforce

      - name: Measure memory baseline
        run: |
          chmod +x scripts/bench-memory-baseline.sh
          scripts/bench-memory-baseline.sh --app build/CocxyTerminal.app --enforce

      - name: Run performance benchmark suite
        run: |
          chmod +x scripts/run-performance-benchmarks.sh scripts/run-cocxycore-benchmarks.sh
          scripts/run-performance-benchmarks.sh
